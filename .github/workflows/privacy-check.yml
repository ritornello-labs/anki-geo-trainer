name: Privacy check

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  tracked-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject live collection snapshots
        run: python3 scripts/check_private_artifacts.py
