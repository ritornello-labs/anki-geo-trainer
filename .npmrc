# npm 11.x does not support a minimum-release-age gate (min-release-age is
# rejected as an unknown key). We instead pin exact versions (save-exact) and
# install through `sfw`; dev deps here are test-only and never ship in the APKG.
save-exact=true
